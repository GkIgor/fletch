output: dist/
releases:
  - name: production
    jobs:
      - name: release-deb
        variables:
          FLAVOR: prod
        package:
          platform: linux
          target: deb
          build_args:
            dart-define:
              FLAVOR: prod
